[project]
name = "hermes-capability-router"
version = "0.2.1"
description = "Hermes MCP capability discovery router"
requires-python = ">=3.11"
dependencies = [
  "mcp",
]

[tool.setuptools.packages.find]
where = ["src"]
