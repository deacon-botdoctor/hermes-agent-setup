# Patch modules — each file exposes an idempotent patch function.
# See ../registry.yaml for metadata and ordering.
#
# The registry is the only authoritative inventory. Historical modules are
# retained in Git history, not alongside the active payload.
